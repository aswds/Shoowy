import {
  createConfig,
  config,
  GluestackUIProvider,
  Button,
  ButtonText,
} from "@gluestack-ui/themed";
import Colors from "./constants/Colors";
export const extendedConfig = createConfig({
  ...config.theme,
});
